#include <stdlib.h>
#include <stdio.h>

/**
 * main - print the name of the file the
 * program is compiled from
 *
 * Return: Always Success (0)
 */

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
